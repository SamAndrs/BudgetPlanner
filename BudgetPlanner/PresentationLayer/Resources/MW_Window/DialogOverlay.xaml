<UserControl x:Class="BudgetPlanner.PresentationLayer.Resources.MW_Window.DialogOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetPlanner.PresentationLayer.Resources.MW_Window"
             xmlns:converter="clr-namespace:BudgetPlanner.PresentationLayer.Resources.Converters"
             >

    <UserControl.Resources>
        <!-- Converter för Income/ Expense color -->
        <converter:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>
    
    <Grid>
      <Border Background="#80000000"
            Visibility="{Binding IsOpen,
                                 RelativeSource={RelativeSource AncestorType=UserControl},
                                 Converter={StaticResource BoolToVisibility}}" />

        <ContentControl Content="{Binding DialogContent,
                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="{Binding IsOpen,
                                 RelativeSource={RelativeSource AncestorType=UserControl},
                                 Converter={StaticResource BoolToVisibility}}" />

    </Grid>
</UserControl>
