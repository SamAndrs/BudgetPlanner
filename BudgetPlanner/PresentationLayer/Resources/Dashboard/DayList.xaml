<UserControl x:Class="BudgetPlanner.PresentationLayer.Resources.Dashboard.DayList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetPlanner.PresentationLayer.Resources.Dashboard"
             xmlns:controls="clr-namespace:BudgetPlanner.PresentationLayer.Resources.Dashboard"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Border CornerRadius="12" Padding="10" Background="{StaticResource SecondaryBackground}"
         Grid.Column="0" Grid.Row="1" Grid.RowSpan="2" Margin="10">
        <StackPanel>
            <!-- Weekdays -->
            <UniformGrid Columns="7" Margin="0,0,0,5">
                <TextBlock Text="MÅN" HorizontalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource SecondaryForeground}"/>
                <TextBlock Text="TIS" HorizontalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource SecondaryForeground}"/>
                <TextBlock Text="ONS" HorizontalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource SecondaryForeground}"/>
                <TextBlock Text="TOR" HorizontalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource SecondaryForeground}"/>
                <TextBlock Text="FRE" HorizontalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource SecondaryForeground}"/>
                <TextBlock Text="LÖR" HorizontalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource SecondaryForeground}"/>
                <TextBlock Text="SÖN" HorizontalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource SecondaryForeground}"/>
            </UniformGrid>

            <!-- Day squares -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">

                <ItemsControl ItemsSource="{Binding Days}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="7"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <controls:DaySummarySquare/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Border>
</UserControl>
