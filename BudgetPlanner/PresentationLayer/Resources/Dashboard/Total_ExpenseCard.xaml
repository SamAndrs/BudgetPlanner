<UserControl x:Class="BudgetPlanner.PresentationLayer.Resources.Dashboard.Total_ExpenseCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetPlanner.PresentationLayer.Resources.Dashboard"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Border CornerRadius="12" Padding="10" Background="{StaticResource SecondaryBackground}">
        <StackPanel x:Name="RootPanel" MinWidth="100">

            <!-- Income card -->
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="↘" 
                      Foreground="{StaticResource ExpenseRed}"
                      FontSize="22"/>
                <TextBlock Text="Utgifter" 
                      Foreground="{StaticResource SecondaryForeground}"
                      FontSize="14"
                      Margin="5"/>
            </StackPanel>

            <TextBlock Text="{Binding TotalExpensesThisMonth, StringFormat='{}{0:C}'}"
                 Foreground="{StaticResource PrimaryForeground}" 
                  FontSize="12" FontWeight="Bold" />
        </StackPanel>
    </Border>
</UserControl>
