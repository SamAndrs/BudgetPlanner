<UserControl x:Class="BudgetPlanner.PresentationLayer.Resources.BudgetPosts.Filter_Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetPlanner.PresentationLayer.Resources.BudgetPosts" xmlns:domain="clr-namespace:BudgetPlanner.DomainLayer.Enums"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <Border Style="{StaticResource CardStyle}" Margin="0,16,0,16">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Datumintervall -->
            <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,24,0" >
                <TextBlock Text="Från" Style="{StaticResource LabelTextStyle}" />
                <DatePicker SelectedDate="{Binding FromDate}" />
            </StackPanel>

            <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,5,24,0" >
                <TextBlock Text="Till" Style="{StaticResource LabelTextStyle}" />
                <DatePicker SelectedDate="{Binding ToDate}" />
            </StackPanel>
            
            <!-- Kategori -->
            <StackPanel Grid.Column="1" Grid.Row="0">
                <TextBlock Text="Kategori" Style="{StaticResource LabelTextStyle}" />
                <ComboBox ItemsSource="{Binding Categories}"
                          DisplayMemberPath="Name"
                          SelectedItem="{Binding SelectedCategory}"
                          Width="200"  Margin="0,5,24,10"
                          Padding="6"
                          HorizontalAlignment="Left"/>
            </StackPanel>

            <!-- PostType filter -->
            <StackPanel  Grid.Column="1" Grid.Row="1">
                <StackPanel>
                    <TextBlock Text="Inkomst/utgift" Style="{StaticResource LabelTextStyle}" />
                    <ComboBox 
                        ItemsSource="{Binding BPTypeFilterOptions}"
                        SelectedItem="{Binding SelectedTypeOption}"
                        DisplayMemberPath="DisplayName"/>
                </StackPanel>

            <!-- Sökfält -->
                <TextBlock Text="Sök" Style="{StaticResource LabelTextStyle}" Margin="0,5,0,0"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Width="200"
                         Padding="6"
                         HorizontalAlignment="Left"/>

                <!-- Ny post -->
                <Button  HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="0,15"
                         Content="Ny budgetpost"
                         Command="{Binding AddNewBudgetPostCommand}"
                         Style="{StaticResource MainButtonStyle}"
                         FontWeight="Bold"
                         Width="200" Height="30" />
            </StackPanel>
            
        </Grid>
        
    </Border>
</UserControl>
