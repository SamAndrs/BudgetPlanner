<UserControl x:Class="BudgetPlanner.PresentationLayer.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetPlanner.PresentationLayer.Views"
             xmlns:controls="clr-namespace:BudgetPlanner.PresentationLayer.Resources.Dashboard"
             xmlns:wpf="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             mc:Ignorable="d"
             >
    <Border Background="{StaticResource PrimaryBackground}" Padding="15">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <!-- TOPBAR -->
            <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" VerticalAlignment="Top">
                <controls:DB_TopBar HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <!-- Income/ Expense cards-->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <controls:Total_IncomeCard />
                    <controls:Total_ExpenseCard Margin="15,0,0,0"/>
                </StackPanel>

            </StackPanel>
           
            <!-- LEFT COLUMN: DAY LIST -->
            <controls:DayList Grid.Column="0" Grid.Row="1" Grid.RowSpan="2" Margin="10"/>
           
            
            <!-- CENTER COLUM: BUDGETPOST LIST -->
            <controls:DB_MonthPostsList Grid.Column="1" Grid.Row="1" Grid.RowSpan="2" Margin="10"/>
            
            <!-- RIGHT COLUMN: -->
            
            <!-- Bar-diagram-->
            <Grid Grid.Column="3" Grid.Row="0" Grid.RowSpan="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Margin="10"
                           Text="Översikt (inkomster/ utgifter)"
                           Style="{StaticResource LabelTextStyle}"/>

                <Border Grid.Row="1"  CornerRadius="12" Padding="10" 
                        Background="{StaticResource SecondaryBackground}" 
                        MinHeight="200">

                    <wpf:WpfPlot x:Name="DailyBarChartPlot"/>
                </Border>

            </Grid>
            
            
            <!-- Pie-chart diagram-->
            <Grid Grid.Column="3" Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Margin="10"
                           Text="Utgifter per kategori (%)"
                           Style="{StaticResource LabelTextStyle}"/>

                <Border Grid.Row="1" CornerRadius="12" Padding="10" 
                        Background="{StaticResource SecondaryBackground}"
       >
                    <wpf:WpfPlot x:Name="PieChartPlot"/>
                </Border>

            </Grid>
            
           

            


        </Grid>
    </Border>
</UserControl>
