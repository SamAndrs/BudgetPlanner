<UserControl x:Class="BudgetPlanner.PresentationLayer.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetPlanner.PresentationLayer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>

            <TextBlock Text="{Binding Title}"
                       FontSize="26"
                       FontWeight="Bold"
                       Margin="0 0 0 20"/>

            <TextBlock Text="Senaste poster:"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0 0 0 10" />

            <ItemsControl ItemsSource="{Binding RecentItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="0 0 0 1" Padding="5">
                            <TextBlock Text="{Binding}" FontSize="16" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>



        </StackPanel>
        
        <!-- 
Innehåll:

Kort med: ”Totala inkomster denna månad”

Kort med: ”Totala utgifter denna månad”

Kort med: ”Saldo”

Mini-prognoswidget: ”Nästa månad: +/− XX kr”

Pie chart: Utgifter per kategori

Snabblänkar: ”Lägg till utgift”, ”Lägg till inkomst”

🧩 WPF UI-komponenter:

ui:Card

ui:ProgressRing

ui:SymbolIcon

NavigationViewItem för routing
-->
    </Grid>
</UserControl>
