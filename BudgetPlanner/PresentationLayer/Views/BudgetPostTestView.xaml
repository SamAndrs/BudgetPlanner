<UserControl x:Class="BudgetPlanner.PresentationLayer.Views.BudgetPostTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetPlanner.PresentationLayer.Views"
             
             xmlns:viewmodels="clr-namespace:BudgetPlanner.PresentationLayer.ViewModels"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewmodels:BudgetPostTestVM />
    </UserControl.DataContext>

    <Grid Margin="20" Background="{DynamicResource SurfaceBrush}">
        <StackPanel Margin="12" VerticalAlignment="Top">

            <TextBlock Text="Test av BudgetPost (Mockad Data)"
                  FontSize="20"
                       Margin="0,0,0,12" 
            />

            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Kategori:" FontWeight="Bold" />
                <TextBlock Text="{Binding TestPost.Category.Name}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Belopp:" FontWeight="Bold" />
                <TextBlock Text="{Binding TestPost.Amount}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Typ:" FontWeight="Bold" />
                <TextBlock Text="{Binding TestPost.TransactionType}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Återkommande:" FontWeight="Bold" />
                <TextBlock Text="{Binding TestPost.Reccuring}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Datum:" FontWeight="Bold" />
                <TextBlock Text="{Binding TestPost.Date, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" />
            </StackPanel>

            <ui:Button 
                Content="Increase Amount"
                Appearance="Primary"
                Command="{Binding IncreaseAmountCommand}" 
                Margin="0,16,0,0" 
                Width="160" />
        </StackPanel>
            
    </Grid>
</UserControl>
